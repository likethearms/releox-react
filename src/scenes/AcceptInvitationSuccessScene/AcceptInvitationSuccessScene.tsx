import { successScene } from '../../HOC/success-scene';
import { routes } from '../../routes';

export const AcceptInvitationSuccessScene = successScene('AcceptInvitationSuccessScene', 'acceptInvitationSuccess', routes.LOGIN);
